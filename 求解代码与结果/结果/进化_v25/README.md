# v25 迭代记录（严格口径重建）

> 本轮目标：对标对方"23 架 / 94.897 min / 67.14 kWh"，自己摸索更优/更稳健版本。
> **重大发现**：并行线已将 evaluate 升级为严格版——**投递区必须等于箱属区**（`bid.split('-')[0] != sid → hard_ok=False`）。此前所有"捎带"（把 A 区箱挂在只飞 B 区的架次上结算投送时刻）被判定为物理不可行。

## 关键结论

1. **26 架冠军（论文口径 6959.8s / 70.71kWh / 零迟到）严格口径不可行**
   - v24（afc4405）26 架解严格 evaluate：`hard=False`，**14 箱区不符**
   - 错区箱全部挂在"单一区条目"架次（如 f24 只飞 S006 却投 S013-WAT-01/S008-MED-01）——无人机从未经过真实区，纯错误挂载
   - 多区化修复尝试（给架次追加真实区段）全部失败：S003↔S013 等区对不顺路，加区段必超能量/超时限
   - **结构结论：26 架方案的捎带是省架次的关键，严格补正至少需要 +2 架次**

2. **严格口径基线 = 28 架 / 8153.2s / 80.31kWh**（并行线最新工作区 p2_results.json，区一致 hard True）

3. **本轮压缩结果（区一致）**：
   - v25b（merge+transfer+split 从 28 架基线出发）：**28 架 / P2 7394.9s（123.2 min）/ 83.12 kWh / hard True / 零迟到**
   - P2 完工较基线 -9.3%；能耗 +2.8 kWh（完工优先方向）
   - P3 端到端：联合完工 **9304.9s（155.1 min）**——中继 W 班 t=[1378,8903] 返场 9322 拖尾，cover_bad=0、R2_overlap=0、mpen=58s
   - 中继能耗 W 2.529 / E 1.549 / N 0.488 = 4.57 kWh（运输跨度 7394s 需更长中继覆盖）

4. **23 架方向（mk_hyper 7722.1s/67.48kWh）不可行**：依赖 33 箱捎带（区混乱遍布 22 架次），严格口径 hard False；且 23 架完工本就 >26 架冠军。

## 与对方对比（严格口径）

| 方案 | 架次 | P2 完工 | 能耗 | 区一致 | P3 联合 | 迟到 |
|---|---|---|---|---|---|---|
| 26 架冠军（v24 afc4405） | 26 | 6896.5s | 70.86 | **✗ 14 箱错区** | — | 0 |
| 并行线基线（28 架） | 28 | 8153.2s | 80.31 | ✓ | **10195.2s** | 459s |
| **v25b（本轮）** | 28 | **7394.9s** | 83.12 | ✓ | **9304.9s (155.1min)** | **0** |
| 对方参考 | 23 | 5694s | 67.14 | 待对方明细 | — | — |

**严格口径下（区一致 + 零迟到 + 联合完工），v25b = 当前最优严格解**：
P2 完工较基线 -9.3%、P3 联合 -8.7%、且基线 SA 后仍有 459s 迟到（v25b 零迟到）。能耗 +2.8 kWh 是"完工优先"的代价（基线为能耗优先）。

## 文件清单

- `p2v25_diag.py` / `结果/进化_v25/p2v25_diag.json`：23 架 mk_hyper s13 解诊断（LB2 拆解 + 可重建解）
- `p2v25_compress.py`：23 架→25 架完工压缩（merge/retype/split/transfer）——产出 6807.2s 但捎带不可行
- `p2v25_repairzones.py`：区一致化修复尝试（失败：真实区专机满载/不顺路）
- `p2v25b_compress26.py`：28 架区一致完工压缩（merge/transfer/split）→ 7394.9s
- `p2v25c_multizone.py`：26 架冠军多区化修复尝试（失败：不顺路超限）
- `p3v25_verify.py`：P3 端到端验证（monkey-patch 官方管线）
- `结果/进化_v25/`：全部输出 JSON

## 未决/下一步

- 对方 23 架 / 94.9min 在严格口径下若成立则显著强于我方所有已知严格解（我方最优 155 min 级）——
  **必须向对方索取逐架 route 明细，确认其投送区与箱属区一致**（若对方同样捎带则不可行；若多区合并则需学其结构）
- 28 架 P3 联合 9305s 中继拖尾 1931s：尝试 SA 压缩中继窗口（w_relay>0）或运输末班提前
- 严格口径下探索"合法多区合并"搜索（一架次真实访问多区投各区箱）——这是对方可能的省架次结构
- 与并行线基线对齐口径（他们 80.31 能耗优先 vs 我方 83.12 完工优先，联合对比我方更优）

## v25.1 追加实验（本地，未推送）

**方法学修正**：P3 SA 结果受 Python 进程 hash 随机化影响（同 seed 两次运行联合完工漂移 9305 vs 10282）。
**全部实验统一 `PYTHONHASHSEED=0` 重跑取确定性口径。**

1. **确定性 P3（v25b 解，seed 7, iters 2500, PYTHONHASHSEED=0）**：联合完工 **9304.9s（155.1 min）** / 零迟到 / cover_bad=0 / R2_overlap=0 / mpen=58s（确认 pwsh-34 口径，10282 为 hash 漂移异常值）。
   中继 W [1378,8903] 2.529 + E [1060,5498] 1.549 + N [6565,7308] 0.488 = 4.57 kWh。
2. **中继拖尾诊断**：SA 把 f4（A 机 S004/N 区 2 箱）推迟 +3600、f22 +2104 → W 任务被挤到 8903。
3. **maxdelay=1200 约束 SA**：联合完工降至 **8573.3s（142.9 min）** 但 machine_pen=2458s（N 班 4165 早于 E 返场 5857，中继 2 不能分身）——**中继排班不可行**（除非 N 班推迟→联合回升）。
   **结构性张力**：S004（N 区）任务窗 [2400,6500] vs 中继 2 的 E→N 接力（E 班 [1060,~5500] 后 N 最早 ~5800）——28 架解零冲突联合完工下限 ≈ 9305s。
4. **完工瓶颈分析**（28 架解链）：U07/U08（C 机）各 4 架次 = 7395 主导；B 链 7108；A 链 6946。
   - v25f 跨机型链优化（C 满载近区拆 A 早班、C 轻载转 B、B 轻载转 A）：**全部失败**——A 机队已饱和（12 架次/4 机），任何转移必延长目标机链。
   - 完工 7394.9 是 28 架结构的机队平衡态 / 强局部最优。
5. **tabu 续搜**（v25g，从 7394.9 出发 600s 完工权重）：无改善（仍 7394.9）。
6. **能耗帕累托**（v25e）：CAP=7450 → 83.01 kWh（仅 -0.11）。**结论：7395 级完工能耗下限 ≈ 83.0 kWh**（完工-能耗权衡显著：8153s→80.31、7395s→83.12）。

**当前严格口径最优（本地）**：28 架 / P2 7394.9s / 83.12 kWh / P3 联合 9304.9s（零冲突、零迟到）。

## v25.2 能耗优先挖掘（用户目标：能耗 60 多 + 完工 6000-7000）

**能耗-完工权衡轨迹**（严格口径、确定性 PYTHONHASHSEED=0）：
| 方案 | 架次 | 运输完工 | P3 联合 | 能耗 | 迟到 |
|---|---|---|---|---|---|
| v25b（完工优先） | 28 | 7394.9 | 9304.9 (155.1 min) | 83.12 | 0 |
| v25i（大合并） | 25 | 7639.0 | - | 78.88 | 0 |
| v25j（能耗优先，冠军） | 24 | **7889.9** | **7939 (132.3 min)** | **77.21** | 18.4s* |
| 对方 | 23 | 5694 | - | 67.14 | ? |

*v25j 加入 P3 后 S004 两箱 deadline7200 尾差 22s（SA 排班推后链序）；P2 运输本身零迟到（7889.9）。

**能耗原因诊断**：
- 能耗 83→77 的主靠**大合并降架次**（28→24，省 4 趟 prep）。每架次省 ~1.1-1.5 kWh。
- v25i 修正（接受 energy↓ 且 完工 ≤ cur+250/400）后：28架/83.12 → 25架/78.88 → 24架/77.41 → P3 联合后 77.21。
- 趟长主因：A 机 2 区趟 2400-2500s（A 慢+多区）；趟均 2077s vs 对方 ~1981s。

**能耗 60 多的不可达性论证**（严格口径）：
1. **A 机 25kg 上限**：80 箱多为 ~11-22kg → A 机物理最多 2 箱/趟 → A 机 12 趟下界（25kg 上限固死），无法 A 内部合并。
2. **B 机 30kg 上限**：2 箱 22kg + 1 箱 22kg = 44 > 30，B 轻载无法内部合并。
3. **C 机能量上限**：M1/M2 定向合并实测——f18 S002+S007(62kg 2区)超 E_use、f23 [S005,S008,S009](70kg 3区)超 E_use → **C 机 80kg 是质量上限、能量上限更紧**，C 机无法大量吸收。
4. **结论**：架次下界 ≈ 22-24 → 能耗下界 ≈ 68-77 kWh。**对方 23 架/67.14 在我方严格口径下不可达**——需向对方索取明细核对投送区一致性/能耗口径（是否含重装、是否捎带）。

**中继结构性结论**（24 架）：SA 3600 → 联合 7939 迟到 18.4s（中继可行 mpen≤111）；maxdelay 1200 → 联合 7639 零迟到但中继冲突 2211s（N 班早于 E 返场）——**24 架解零冲突零迟到联合下限 ≈ 7939s**。
