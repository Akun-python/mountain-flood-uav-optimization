# 对方方案（23 架 / 94.897 min / 67.14 kWh）去向——正式明细索取与复核清单

**致：对方队伍**
**发：我方 D 题协同优化组**
**理由：为保证两队结果可比，需在同一口径下复核（竞赛题明确"公共物理规则、单位、对象编号和计算口径应保持一致"）**

## 一、我方口径声明（与题目附录 2/3 及数据文件完全一致）

我方模型严格采用以下规则：
- 投送位置 = 货箱所属服务区（一架次"访问某区"才可在该区投该区箱）——即**投箱必须在该架的 route 真实经过该区**
- 返航安全余量 `E_p^T ≤ (1-ρ_g)·E_g^use`（运输）及对应中继规则
- 共享电池 A6/B4/C4 独立周转，两阶段充电 `t_chg(s)`（90% 阈值、65/35 分段）—— 任务占用与充电时段不得重叠
- 中继悬停：DEM 覆盖范围内，离地高度 ≤ 300 m，连续通信（附录3 链路预算）
- 全部任务完成时间 = 所有运输无人机完成最后一架次返回 O01 的最晚时刻

**我方已达成基线：**
| 指标 | 完工优先 | 能耗优先 |
|---|---|---|
| 架次数 | 28 | 24 |
| 运输完工 | 7394.9 s (123.2 min) | 7889.9 s |
| P3 联合完工 | 9305 s | 7939 s |
| 运输能耗 | 83.12 kWh | 77.21 kWh |
| 迟到 | 0 | 轻微（可消） |

## 二、我们为何需要逐架明细（四项核对）

对方 23 架 / 94.89 min / 67.14 kWh 在我们严格口径下**当前不可复现**（我方完工下界 ≈ 7395 s、架次下界 ≈ 24、能耗约 77 kWh）。为公平判断对方方案是否成立、非捎带/非口径差异，恳请对方提供**竞赛结果提交模板**的四张表（Q2_运输架次、Q2_逐箱下行、Q3_中继架次、Q3_通信保障）——若此前已按模板提交过即可直接提供。

| # | 核对项 | 需对方提供 | 我方核验方法 |
|---|---|---|---|
| 1 | **投送区一致性**（关键） | Q2_逐箱下行：箱→区→交付时刻 | 每个箱的"投送服务区"必须=附件该箱属区；陪途末箱是否经?（若某箱只在它机回路上"虚拟"投送 = 物理可取） |
| 2 | **能耗口径** | 每架能耗（Q2/Q3） | 按附录2 公式重算（含爬升效率0.72、交接、返航余量）；确认是否含中继/充电损失/备用 |
| 3 | **电池周转** | 每架电池编号+开始/返回时刻（Q2） | 逐电池链条：SOC 变化→两阶段充电→下一架最早已最 ready；数量 ≤ A6/B4/C4，且不重叠 |
| 4 | **完工-架次实现** | 可重构：各机链趟序（Q2 聚合） | 8 机 ≤2.9 趟/机×~1960s 才能到 5694s：逐机链趟长合计验证 |

## 三、数据交换约定（最省贵方力气）

若你方用**结果提交模板.xlsx** 或 JSON（任一含上述四表字段）提供即可。我们收到后运行**自动复核脚本**（`verify_opponent.py`），逐项输出：投送区一致、能耗重算、电池 SOC/充电周转、中继位置/高度/覆盖、各机链趟数与完工推导——并在 24h 内回传一份"对照复核报告"（含我们双方指标能否对的说明）。

所有提供数据仅供公平核验，不外传、不用于除 D 题对比之外的任何场景。

**恳请拨冗提供。共同维护竞赛结果的统一口径。感谢！**